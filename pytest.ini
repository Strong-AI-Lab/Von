[pytest]
asyncio_mode = auto

# Only collect the real test suite.
# Repo root contains various test_*.py helper scripts which are not pytest tests.
testpaths =
	tests

# Ensure the top-level 'src' package resolves.
pythonpath =
	src
