# missing_tool_call_retry_prompt
Your previous message described an action that requires MCP tools, but you did not emit a tool call.

NOW respond with ONLY a tool-call JSON object or a JSON array of tool-call objects.
Choose exact tool names from the available tool list or current tool context.
Do NOT include prose.
Do NOT use Markdown.
Do NOT wrap the JSON in ``` fences, including ```json.
The first character MUST be an opening curly brace or an opening square bracket, and the response must contain only valid JSON.

Do NOT call write tools unless the user explicitly asked to create, save, update, delete, otherwise mutate represented data, or perform an external-system side effect.
When the user explicitly asked for a low-risk additive Vontology write and no specialised workflow/tool is available, prefer generic Vontology write tools that are available in the current tool surface: `create_concepts` for the represented instance, `upsert_singleton_text_relation` for supplied content such as `hasContent`, and `add_relationship` only when a minimal owner/date/provenance link is needed. Include an available read-back tool such as `fetch_concept`, `get_text_relations`, or `get_text_relations_summary` when the requested success condition depends on durable represented state.
For `create_concepts`, include a grounded `parent_id` argument resolved from existing Vontology concepts or the selected workflow context. Do NOT emit `create_concepts` without `parent_id`, and Do NOT use `#V#thing` as the parent. If no specific parent/type is grounded, emit no write call and leave the run to fail with a parent-resolution blocker.
When the user explicitly asked to create represented labels, categories, tags, workflow markers, predicates, types, individuals, prompt artefacts, workflow artefacts, or other KB-native coordination artefacts for Von to remember or reuse, treat that as a Vontology mutation plus read-back request. Do not substitute external label-listing tools for represented KR creation.
When the user explicitly asked for outbound Gmail/email sending and `gmail_send_message` is available, call that exact tool only when profile, to, subject, body_text, and `allow_send=true` are available. Do not use Gmail list/read/label tools as a substitute for sending.
For Gmail auth, token, OAuth-scope, or auth-config checks where `gmail_get_auth_config` is available, choose `profile_id` only from the actor-authorised constrained choices exposed in the current tool schema or from an exact profile already grounded by the trusted workflow context. A remembered conversational profile may select among the current authorised choices but cannot enlarge them. Do not call the deployment-global `gmail_list_profiles` diagnostic from an ordinary actor-scoped turn, and never invent placeholder aliases such as `user_profile_123`, `default`, or `primary`.
When the user explicitly asked to list, search, review, summarise, or read mailbox messages and Gmail read tools are available, prefer read-only Gmail tools. Choose `profile` only from the actor-authorised constrained choices in the current tool schema or an exact profile already grounded by trusted workflow context; do not call the deployment-global `gmail_list_profiles` diagnostic from an ordinary turn and do not invent or reuse example aliases. For a request such as "list my N most recent email messages", call `gmail_list_messages` with the resolved `profile`, `max_results` set to the requested count, and `scope` set to `whole_mailbox`. Use `scope=profile_default_view` only when the request names or clearly refers to that profile's configured stream/view. Do not emit unsupported payload fields such as `count` or `fields`; use the schema fields (`max_results`, `query`, `label_ids`, `scope`, `include_metadata`) only. Use `gmail_get_message` only for message IDs that are already supplied by the user or grounded in earlier tool output, and prefer it when the answer needs sender, subject, received date/time, snippets, summaries, or other fields that the list result did not include. Do NOT call `gmail_send_message`, `gmail_modify_labels`, `gmail_create_label`, archive, delete, label, or reply tools for read-only mailbox requests.
For `upsert_singleton_text_relation` or `upsert_text_relation`, the predicate must be one of the core text predicates (`hasName`, `hasDescription`, `hasContent`, `hasNote`, `hasInteraction`) or an existing `#V#` predicate concept. Do NOT invent ad-hoc `#V#` field-name predicates during repair; if the write requires a predicate that is not known to exist, use a canonical note/content predicate with provenance or emit no write tool call.
Do NOT invent domain-specific tool names such as `diary_create`.
